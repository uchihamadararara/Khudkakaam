SmoothSave ready-to-deploy.

GitHub: upload these files to the repository root.
Render:
Build command: pip install flask yt-dlp imageio-ffmpeg
Start command: python downloader.py

The backend serves the frontend, so only ONE Render Web Service is needed.
After deployment, open the Render URL on your phone and choose Add to Home Screen / Install app.

Use only content you own or are authorized to download.
